Confirmed: 12:55pm and 6:31pm Toronto today, exactly matching Replit's numbers. Since the rewrite never got pushed (blocked the whole time), those runs are the old workflow — the one I recommended reverting to. Replit's core point stands: reverting would restore the exact thing that already failed this morning, not fix anything.

But there's a bigger problem underneath both our analyses, and it's worse than either of us said. The old workflow's cron is */10 11-14,19-22 * * * — every 10 minutes across two 4-hour UTC windows. That should produce roughly 48 scheduled runs today. GitHub's own API shows exactly 2. That's not "occasionally delayed" — that's GitHub silently dropping something like 95% of the scheduled triggers. GitHub explicitly documents scheduled Actions workflows as best-effort, not guaranteed, and this is a known failure mode for anything scheduled frequently — but I hadn't actually pulled the numbers until you pushed back, and seeing a 46-of-48 drop rate changes my recommendation, not just my explanation.

Given that, I don't think Replit's fix fully solves this either — going from ~48 possible trigger opportunities down to 2 (one per window) removes almost all redundancy. If GitHub drops that one trigger the way it dropped 46 of today's 48, you get zero coverage for that entire window instead of a near-miss. The corrected timeout/exit logic is a real improvement worth keeping regardless, but the underlying reliability problem — GitHub's own scheduler, not either version's internal logic — is the actual root cause, and no amount of cron-script cleverness fixes it.

Two honest paths forward, and I'd genuinely recommend the first over continuing to iterate on GitHub Actions:

Better stopgap: swap GitHub Actions' scheduler for a purpose-built uptime/cron ping service — something like cron-job.org, UptimeRobot, or Better Stack. These exist specifically to hit a URL on a reliable schedule and don't share GitHub's best-effort scheduling model. Free tiers on all of them support pinging every 5 minutes. This is a smaller change than it sounds: same /api/health endpoint, no code logic changes needed, just point an external service at it instead of GitHub Actions. I'd genuinely do this before spending more time patching the GitHub Actions script.

The actual fix, unchanged from when we first discussed this: move off Autoscale to a Reserved VM. Today's data is a concrete, measured example of exactly the failure mode I flagged back then — this isn't a hypothetical risk anymore, you have the numbers in front of you.

Given today's evidence, my honest advice: do the external ping service now (it's a 10-minute setup, not a rebuild), and treat the Reserved VM migration as less optional than "eventually" — worth moving up your timeline rather than waiting for a specific user count.